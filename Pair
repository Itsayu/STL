#include<bits/stdc++.h>
using namespace std;

void explainPair(){
  pair<int, int>p = {1, 7};
  cout<<p.first<<" "<<p.second<<endl;
  
  pair<int, pair<int,int>>nr = {22, {11, 10}};
  cout<<nr.first<<" "<<nr.second.first<<" "<<nr.second.second;
}

int main(){
  explainPair();
}
